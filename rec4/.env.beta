# Beta Environment Configuration
# Copy this to .env when running beta environment

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=booksdb_beta
DB_USER=postgres
DB_PASSWORD=postgres_beta_secure_password
